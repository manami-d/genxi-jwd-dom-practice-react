// Buttons
// When the button is clicked, set the buttonInvisible state

// Mouse Over
// When the mouse enters the image "light-bulb", change
// the "src" attribute of the image to "images/light-bulb-on.png"
// When the mouse leaves, change the "src" attribute to "images/light-bulb-off.png"

// Toggle
// Clicking the button with id "toggle-button" should toggle the visibility
// of the alert with id "toggle-alert" on and off.

// Validate
// When the form is submitted,
// - prevent the default event from firing
// - set the input value to state
// - check the length of the input 
// - if the length is greater than 2, set some text in the div
// - if the length if 2 or less, add text to tell the user how many characters it should be

// Challenge: Lists
// When the form with the id "form-hobby" is submitted
// - prevent default events from firing
// - get the value from the input
// - add the value to the array of tasks
// - use tasks array to create a list of the taks in the <ul> provided
// - try using .map()

